# VERIFIED-WORKING versions, as installed in the `dino_wm` conda env (2026-09).
# These are what every result in NOTES.md was produced with -- not minimum bounds.
#
# CUDA: torch must be a cu128 build. Both GPUs used here are Blackwell (RTX 5060 Ti,
# RTX 5090 = sm_120) and older cu121 wheels will not run on them. Check with:
#     python -c "import torch; print(torch.cuda.get_arch_list())"   # expect sm_120
#
# NOTE: this file previously pinned `numpy<2`, which was WRONG -- torch 2.11 here runs
# against numpy 2.4.6. Following the old pin breaks a fresh install.

# python 3.11.15
torch==2.11.0            # +cu128 build; install from the PyTorch cu128 index
torchvision
numpy==2.4.6
pillow==12.2.0
matplotlib==3.11.1
imageio
imageio-ffmpeg
opencv-python
einops                   # required by dino_wm's model code
hydra-core==1.3.2        # dino_wm configs
omegaconf
pytest                   # tests/
